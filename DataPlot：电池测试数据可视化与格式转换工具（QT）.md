# DataPlot (数据绘图分析与格式转换工具)

**DataPlot** 是一款基于 **PySide6 (Qt6)** 与 **Matplotlib** 深度协同开发的高性能数据可视化、微分分析及格式转换工具。软件专为工程仿真（如 FLOEFD 热流分析）、环境采集及电池充放电测试等海量实验数据的快速清洗、波形绘制、多维对比与规范化导出而设计，提供学术级图表渲染能力与极速数据处理体验。

---

## 🖼️ 软件界面与可视化效果

| ![[DataPlot：电池测试数据可视化与格式转换工具（QT）-7.png]] |
| :--------------------------------------: |
|       多维度交互式绘图系统（支持多 Y 轴自定义与图例智能清洗）       |

| ![[DataPlot：电池测试数据可视化与格式转换工具（QT）-1.png]] |
| :--------------------------------------: |
|           电池测试数据载入（循环筛选、工步筛选与通道映射）           |

| ![[DataPlot：电池测试数据可视化与格式转换工具（QT）-8.png]] |
| :--------------------------------------: |
|                电池循环数据纵向比对（常规对比）                |

| ![[DataPlot：电池测试数据可视化与格式转换工具（QT）-9.png]] |
| :--------------------------------------: |
|        电池循环数据纵向比对（工步筛选：充电、放电、静置）        |

| ![[DataPlot：电池测试数据可视化与格式转换工具（QT）-10.png]] |
| :---------------------------------------: |
|          电池循环数据纵向比对（X 轴为容量，Y 轴为电压）         |

| ![[DataPlot：电池测试数据可视化与格式转换工具（QT）-11.png]] |
| :---------------------------------------: |
|             dQ/dV 曲线绘制（去重/原始/对比与多阶滤波）            |

---

## 🌟 核心功能特性

### 1. 专业级多维度交互式绘图系统

* **多坐标轴独立控制**：支持多达 3 个独立的 Y 轴坐标系（Y1、Y2、Y3），各轴可独立配置线型（实线、虚线、点划线）、数据标记（Marker）及专属调色方案。
* **学术级渲染规范**：默认采用加粗坐标边框与内向刻度线设计；字体、字号、图例位置及图例列数均可实时调控与持久化保存。
* **高响应交互体验**：内置 Matplotlib 交互控制台，支持局部放大、平移、坐标数值悬停提示及无缝恢复初始视角。
* **高级排版参数自由调控**：
  * **标题间距（Labelpad）**：新增 `标题间距` 参数，实现所有 Y 轴标题与刻度线内外的精确距离控制。
  * **解封边缘留白**：高级配置中支持根据像素或百分比完全自由调控画板左右边距，适应各类出版级论文排版需求。

### 2. 电池测试数据与微分分析（BATTERY 面板）

* **智能通道映射与极性转换**：
  * 载入文件后自动匹配“循环号”、“工步号”、“时间”、“电压”及“电流”等关键通道。
  * 支持输入电压与电流的单位缩放比例因子（如将 `mA`、`mV` 自动换算为标准 `A`、`V`）。
  * 内置“CC极性”判定算法，通过基于时间步长（TimeStep）的电流时间积分自动求算充放电容量。
* **相对工步时间重算**：在筛选特定工步（如恒流充电、恒压充电）时，自动将初始时刻重置为 `0`，方便横向对比不同循环段的动态特征。
* **微分容量（dQ/dV）与微分电压（dV/dQ）深度分析**：
  * **三数据处理模式**：
    * **【去重】**：自动剔除电位重合静态点，重排序后在匀速电位网格（$V_{grid}$）上插值求导，消除极化与静态段噪点。
    * **【原始】**：基于原始采样点阵直接求导，完整呈现实测细节。
    * **【对比】**：实现单圈双线同屏对比（去重虚线 `--` 与原始实线 `-`），并在 25 种预设色盘中为每圈自动分配 2 个独立颜色。
  * **自动电位区间识别**：自动扫描全区电位最值并在 Vmin/Vmax 窗口以占位符展示，支持手动重置计算边界。
  * **多阶平滑滤波**：集成移动平均、中值滤波及 Savitzky-Golay (SG) 高阶滤波算法，针对短工步提供自适应窗口缩减与防崩溃安全机制。
  * **副 Y 轴统计折线**：支持在 dQ/dV 界面右侧挂载均值或方差统计趋势线。

### 3. 高度优化的图形界面与交互体验 (PySide6 / Qt6)

* **现代化 Qt6 架构**：采用 PySide6 (Qt6) 原生 GUI 框架，完美兼容 4K/高分屏 DPI 自适应缩放，界面元素清晰挺拔。
* **左右双栏可拖拽布局 (QSplitter)**：采用弹性分割条设计，用户可自由拖拽调整参数控制面板与绘图画布的宽度比例。
* **列表框极简选择交互 (CustomListWidget)**：
  * **单键反转切换（Toggle Selection）**：鼠标左键再次点击已选中的列名即可直接取消选择，无需按住 `Ctrl` 键。
  * **关键字实时搜索**：Y1-Y3 列表顶部配置搜索框，输入关键字实时过滤目标列名。
  * **右键快捷菜单**：支持 `✖ 取消选择此项`、`🔄 反向选择` 及 `🗑 清空当前列表`。
* **无闪烁重绘机制**：重绘过程采用 `setUpdatesEnabled` 冻结渲染技术，彻底消除图表更新时的闪烁感。

### 4. 极速 CSV 转 Excel (csv2xlsx) 转换引擎

* **超低内存占用**：基于 Rust 编写的 Polars 批读取流与 `xlsxwriter` 物理内存模式（`constant_memory=True`）。针对超 1GB、包含数百万行的巨型 CSV 数据，可在几秒钟内极速转换为 Excel 格式。
* **智能平滑分表**：严格遵循 Excel 单表 `1,048,575` 行上限，超出部分自动平滑切分保存至 `Sheet2`、`Sheet3` 等独立工作表中。

### 5. 规范化数据导出与文件管理

* **干净图例标题导出**：保存绘图数据时，自动将列头清洗为简洁的图例名称（自动去除原始通道中的单位括号或冗余前缀）。
* **面板独立文件命名**：
  * FLOEFD 面板 $\rightarrow$ `FLOEFD_Plot_Data.xlsx`
  * GENERAL 面板 $\rightarrow$ `GENERAL_Plot_Data.xlsx`
  * BATTERY 面板 $\rightarrow$ `BATTERY_Plot_Data.xlsx`
  * 电池循环对比 $\rightarrow$ `BATTERY_Cycle_Compare_Data.xlsx`

---

## 📂 项目代码架构与模块说明

项目整体结构严谨，各业务模块解耦清晰：

```
DataPlot_QT/
├── DataPlot_main.py             # 程序主入口脚本 (DPI 适配、GUI 实例化)
├── DataPlot_app_gui.py           # PySide6 主界面组件布局与交互事件绑定
├── DataPlot_data_loader.py       # 多引擎数据解析 (Polars/Pandas) 与 csv2xlsx 转换
├── DataPlot_battery_math.py      # 电池容量积分、相对时间重算与循环范围解析
├── DataPlot_plot_engine.py       # Matplotlib 绘图引擎 (多 Y 轴、dQ/dV 计算与滤波)
├── DataPlot_excel_exporter.py    # 清洗数据与多循环对比数据导出模块
├── DataPlot_settings_manager.py # 用户首选项与界面配置 JSON 持久化管理
├── DataPlot_gui_exe.py           # PyInstaller 编译配置文件
├── Build_DataPlot_Setup.py       # 一键打包与 Inno Setup 安装包生成脚本
├── setup.iss                     # Inno Setup 编译器描述脚本
├── settings.json                 # 默认配置文件
└── icon.ico                      # 应用程序图标
```

---

## 📂 支持的数据面板类型

| 面板类型          | 适用场景                                         | 支持的数据特征与清洗机制                                                                                                                                                                                   |
| :---------------- | :----------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **FLOEFD**  | 热仿真工具导出的原始 CSV/Excel 数据              | 支持**智能列过滤**，自动丢弃仿真多余的重复时间列（仅保留首列时间）；支持设置 `SKIP`（跳过文件表头信息行，默认 3）与 `NULL`（跳过全空行，默认 8）。自动清洗删除图例中的 “温度” 后缀。                 |
| **GENERAL** | 通用实验、传感器采集或自定义清洗后的数据         | 单一时间轴多数据通道。保留 `起始行` 选项（默认 1）精准定位表头行。支持自定义 `SKIP` 和 `NULL` 提高文件结构兼容性。                                                                                       |
| **BATTERY** | 蓝电、新威、Arbin 等电池测试设备导出的充放电文件 | 智能多通道映射；支持多循环纵向常规对比与微分容量（dQ/dV）/微分电压（dV/dQ）对比分析；自动翻转极性积分容量；支持手动输入**电压比例**和**电流比例**（例如输入 `0.001` 将 mA 和 mV 转换为标准 A 和 V）。 |

---

## 📖 使用指南

### 1. 数据载入与格式转换
* **巨型 CSV 转换**：若原始文件为超大 CSV 格式，点击界面顶部 **csv2xlsx** 按钮，系统将在后台极速转换为 `.xlsx` 文件并自动载入。
* **参数配置与读取**：设置 `起始行`、`SKIP` 及 `NULL` 参数后，点击 **读取** 按钮，系统将自动解析工作簿中的工作表并提取通道列名。

### 2. 电池数据配置与工步/循环筛选 (BATTERY 面板)
* 确认 **循环列**、**工步列**、**时间列**、**电压列** 和 **电流列** 映射。
* 设置 **工步筛选**（如仅分析“恒流充电”）与 **循环筛选**，点击 **应用** 刷新数据切片。

### 3. 普通多 Y 轴波形绘制
* 在取消勾选“启用循环对比”状态下，在 **X轴** 下拉框选择横坐标。
* 在 **Y1**、**Y2**、**Y3** 列表中选择需要绘制的数据通道。在列表中**直接点击已选项目即可快速取消选择**，或通过右键菜单进行反选/清空。
* 点击 **绘制图表** 即可生成学术级多 Y 轴曲线。

### 4. 循环对比与 dQ/dV 微分容量分析
* 勾选 **启用循环对比**。在对比范围中输入目标循环段（如 `1, max, 10` 或 `1, 5, 10`）。
* 在 **比对类型** 中选择 `dQ/dV` 或 `dV/dQ`。
* 选择数据处理模式（`去重` / `原始` / `对比`），并配置滤波算法及参数。
* 点击 **应用** 即可生成平滑无毛刺的微分容量对比曲线。

### 5. 图形保存与数据导出
* 点击 **保存数据** 按钮，系统将自动把画布上对应的清洗后数据导出为规范的 Excel 文件。

---

## 🛠️ 源码运行与环境配置

```bash
# 1. 安装核心依赖环境
pip install PySide6 pandas polars openpyxl xlsxwriter python-calamine matplotlib scipy

# 2. 启动应用程序
python DataPlot_main.py
```

---

## 📦 客户端一键打包发布 (Windows)

项目内置一键自动化构建脚本，直接运行：

```bash
python Build_DataPlot_Setup.py
```

该脚本将自动执行以下流程：
1. 调用 PyInstaller 按照精简策略生成绿色可执行程序至 `dist/DataPlot/`；
2. 自动定位系统中的 Inno Setup 编译器，一键编译生成桌面安装向导包 `installer_output/DataPlot_Setup.exe`。